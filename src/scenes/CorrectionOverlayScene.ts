import Phaser from 'phaser';

/**
 * CorrectionOverlayScene — Post-round mistake review.
 * Stub for Track G (deferred Cluster C).
 */
export class CorrectionOverlayScene extends Phaser.Scene {
  constructor() {
    super({ key: 'CorrectionOverlayScene' });
  }

  create(): void {
    // Deferred — see PRD Section 3, Cluster C and Track G (step G.1)
  }
}
